import { HomePage } from '@/components/public/HomePage';

// Public landing page for the HDP portal. All composition lives inside
// components/public/HomePage so that the same content can be embedded in
// other layouts later if needed.
export default function PublicHomePage() {
  return <HomePage />;
}
